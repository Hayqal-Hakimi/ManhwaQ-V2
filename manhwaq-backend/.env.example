NODE_ENV=development
PORT=3000
JWT_SECRET=change-this-to-a-long-random-secret-in-production
CORS_ORIGIN=http://localhost:5173

# --- Database (local dev) ---
DATABASE_PATH=./data/manhwaq.db
# PROD (AWS RDS): DATABASE_URL=postgresql://user:pass@host:5432/manhwaq

# --- AWS (optional until deploy; no code change needed) ---
AWS_REGION=us-east-1
AWS_S3_BUCKET=
# Store cover_url as full S3/CloudFront URL in DB — not local files
