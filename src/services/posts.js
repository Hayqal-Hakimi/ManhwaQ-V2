import { emptyList, emptyItem } from './api';

export const getPosts = async () => emptyList();

export const createPost = async () => emptyItem('API belum disambung.');

export const addComment = async () => emptyItem('API belum disambung.');

export const deleteComment = async () => emptyItem('API belum disambung.');

export const reactToPost = async () => emptyItem('API belum disambung.');
